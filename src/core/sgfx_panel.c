/*
 * sgfx_panel.c
 *
 * Rotation state is implemented in core/gfx_core.c.
 * This file intentionally contains no public symbols; keeping it as a
 * placeholder avoids breaking existing source-tree references while preventing
 * duplicate sgfx_set_rotation() definitions when PlatformIO compiles src/ recursively.
 */
#include "sgfx.h"
